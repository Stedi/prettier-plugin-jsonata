/**
 * Imports types for jest-extended
 */
import "jest-extended";
/**
 * Augments the jest matches with the ones exported from jest-extended
 */
import "jest-extended/all";
import "jest-matcher-specific-error";
